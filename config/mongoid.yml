production:
  clients:
    default:
      database: rtable_live
      hosts:
        - localhost:27017

staging:
  clients:
    default:
      database: rtable_staging
      hosts:
        - localhost:27017

development:
  clients:
    default:
      database: rtable_dev
      hosts:
        - localhost:27017

test:
  clients:
    default:
      database: rtable_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1

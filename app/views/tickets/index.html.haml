%h1 Listing tickets

%table
  %thead
    %tr
      %th Name
      %th Slug
      %th
      %th

  %tbody
    - @tickets.each do |ticket|
      %tr
        %td= ticket.name
        %td= ticket.slug
        %td= link_to 'Show', ticket
        %td= link_to 'Edit', [:edit, ticket]

%br

= link_to 'New Ticket', [:new_ticket]

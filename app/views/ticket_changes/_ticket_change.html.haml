= turbo_frame_tag ticket_change do
  %p= "#{ticket_change.author.name} changed this from <del>#{ticket_change.old_name}</del> to <b>#{ticket_change.name}</b> on #{ticket_change.published_at}".html_safe

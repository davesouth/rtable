= form_with model: card do |f|
  - display_form_errors(f.object)

  %p.field
    = f.label :name
    = f.text_field :name

  %p.field
    = f.label :email
    = f.email_field :email

  %p.actions
    = f.submit 'Save'

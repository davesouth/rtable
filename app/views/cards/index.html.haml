%h1 Listing cards

%table
  %thead
    %tr
      %th Name
      %th Slug
      %th Num
      %th
      %th

  %tbody
    - @cards.each do |card|
      %tr
        %td= card.name
        %td= card.slug
        %td= card.num
        %td= link_to 'Show', card
        %td= link_to 'Edit', [:edit, card]

%br

= link_to 'New card', [:new_card]
